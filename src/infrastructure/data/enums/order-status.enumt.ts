export enum OrderStatus {
    PENDING = 'pending',
     ACCEPTED = 'accepted',
    IN_PROGRESS = 'in_progress',
    PICKED_UP = 'picked_up',
    DELIVERED = 'delivered',
    COMPLETED = 'completed',
    CANCELLED = 'cancelled'
}

export enum OfferStatus {
    PENDING = 'pending',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected'
}